apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: {name: orchestrator-default-deny}
spec:
  podSelector:
    matchLabels: {app: ml-docker-orchestrator}
  policyTypes: [Ingress, Egress]
  ingress:
    - ports:
        - {protocol: TCP, port: 8080}
