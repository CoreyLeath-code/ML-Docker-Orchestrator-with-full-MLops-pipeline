name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          # Explicitly installing testing and validation dependencies
          pip install pytest pytest-cov mypy httpx
          
      # Quality Gate 1: Linting
      - name: Run Linting (Ruff)
        uses: astral-sh/ruff-action@v1
        with:
          args: "check . --fix"
          
      # Quality Gate 2: Formatting
      - name: Run Formatting Check (Ruff)
        uses: astral-sh/ruff-action@v1
        with:
          args: "format ."
        
      # Quality Gate 3: Type Safety
      - name: Run Strict Typing (Mypy)
        run: mypy .
        
      # Quality Gate 4: Test Suite
      - name: Run Test Suite
        run: pytest --cov=. --cov-fail-under=80
